# initial-command = /bin/zsh -c "tmux attach\\; choose-tree -Zs || read '?Create a default session?(y/n)' anwser && [[ \"${anwser}\" == \"y\" ]] && tmux"

initial-command = /bin/bash -c "tmux attach || tmux new-session || exec /bin/bash"
#initial-command = /bin/bash -c "tmux attach\\; choose-tree -Zs || read '?Create a default session?(y/n)' answer && [[ \"${answer}\" == \"y\" ]] && tmux"

# Turn off ligatures
font-feature=-calt
font-feature=-liga
font-feature=-dlig
